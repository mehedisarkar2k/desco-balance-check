# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here

# DESCO Account Details (Admin/Default)
DESCO_ACCOUNT_NO=your_account_number_here
DESCO_METER_NO=your_meter_number_here

# MongoDB Configuration (use either DB_URL or MONGODB_URI)
DB_URL=mongodb+srv://username:password@cluster.mongodb.net/desco-balance-check
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/desco-balance-check

# Environment Configuration
# Use 'development' for local testing, 'production' for Render
NODE_ENV=development

# Render Configuration (for keep-alive)
# This should be your Render service URL (e.g., https://your-app.onrender.com)
RENDER_EXTERNAL_URL=https://your-app-name.onrender.com

# Server Port (Render will set this automatically)
PORT=10000

# IMPORTANT: To avoid conflicts
# - When running LOCALLY: Set NODE_ENV=development and STOP your Render deployment
# - When running on RENDER: Set NODE_ENV=production and DON'T run locally
